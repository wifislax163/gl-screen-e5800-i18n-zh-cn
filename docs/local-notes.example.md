# Local notes (do not commit)

Copy this file to `local/notes.md` (the `local/` directory is gitignored).

Use it for private reminders, test router IPs, serial numbers, or release checklists that are not for the public repo.
